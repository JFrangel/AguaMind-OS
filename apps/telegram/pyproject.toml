[project]
name = "agentos-telegram"
version = "0.1.0"
description = "Telegram bot for AgentOS"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot>=21.0",
    "httpx>=0.28",
]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"
