// Short alias for /previewagentos. The chat header and most internal
// links point to /preview because it's shorter; both URLs return the
// same HTML so external bookmarks keep working.
export { GET } from "../previewagentos/+server";
